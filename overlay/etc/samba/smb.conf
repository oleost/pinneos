[global]
	workgroup = WORKGROUP
	server string = PinneOS
	security = user
	map to guest = Bad User
	guest account = nobody
	unix charset = UTF-8
	log file = /var/log/samba/%m.log
	max log size = 50

# Shares managed by PinneOS — use the Cockpit Shares tab to add/remove
